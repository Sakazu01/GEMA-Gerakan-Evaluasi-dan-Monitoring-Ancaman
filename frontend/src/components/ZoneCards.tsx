"use client";

// TODO Checkpoint 3: dua kartu (arahan keselamatan + konfirmasi bantuan), hanya render saat
// `in_red` true dari POST /api/nearby (PRD §6.1, §16.2). Keduanya bisa diminimalkan, tidak
// boleh menutupi atribusi peta atau tombol aksi utama.
export function ZoneCards() {
  return null;
}
